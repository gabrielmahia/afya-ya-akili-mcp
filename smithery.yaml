# Smithery configuration for afya-ya-akili-mcp
# East Africa AI Coordination Infrastructure — AI-KungFU
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({
      command: "afya-ya-akili-mcp",
      args: [],
      env: {}
    })
